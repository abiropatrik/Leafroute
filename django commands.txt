project létrehozása: django-admin startproject your_project_name

szerver futtatás: python manage.py runserver

migration create: python manage.py makemigrations
migrate: python manage.py migrate
régi felesleges cuccok törlése:  make compose-manage-py cmd="remove_stale_contenttypes"


admin account create: python manage.py createsuperuser

django python commands: docker-compose -p projectContainer run --rm website python manage.py makemigrations

user permission check in html:  <!--{% if perms.accounts.organiser_tasks %}-->
new app creation: python manage.py startapp internal

nem default database-re migrálás:
 make compose-manage-py cmd="migrate --database=stage --run-syncdb"
-->make compose-manage-py cmd="migrate internal_stage --database=stage"

ezeket használtam az init.sqlhez:
    docker-compose down
    docker volume rm $(docker volume ls -qf name=mysql_data)
    docker-compose up --build